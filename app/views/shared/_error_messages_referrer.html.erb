<% if @referrer.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      <%= pluralize(@referrer.errors.count, "error") %>.
    </div>
    <ul>
    <% @referrer.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>