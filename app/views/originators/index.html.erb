<h1>Listing originators</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @originators.each do |originator| %>
  <tr>
    <td><%= link_to 'Show', originator %></td>
    <td><%= link_to 'Edit', edit_originator_path(originator) %></td>
    <td><%= link_to 'Destroy', originator, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Originator', new_originator_path %>
